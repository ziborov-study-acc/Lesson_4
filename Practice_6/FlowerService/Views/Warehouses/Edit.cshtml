@using FlowerServiceLibrary;
@model FlowerServiceLibrary.Warehouse;

<h1>Редактирование информации о складе</h1>
<hr />
<div class="row">
    <div class="col-md-10">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Address" class="control-label"></label>
                <input asp-for="Address" class="form-control" />
                <span asp-validation-for="Address" class="text-danger"></span>
            </div>
            <h4>Цветы на складе</h4>
            <div class="form-group">
                <div class="list-group">
                    @if (Model.WarehouseFlowers.Count() == 0)
                    {
                        <div class="list-group-item clearfix">
                            <h4 class="text-danger"> Цветы на складе отсутствуют</h4>
                        </div>
                    }
                    else
                    {
                        foreach (WarehouseFlower warehouseFlower in Model.WarehouseFlowers)
                        {
                            <div class="list-group-item">
                                @warehouseFlower.Flower.Name
                                <span class="float-right">
                                    <a asp-action="FlowerDetails" asp-route-placeId="@warehouseFlower.PlaceId" asp-route-flowerId="@warehouseFlower.FlowerId" class="btn btn-info">Подробности</a>
                                    <a asp-action="FlowerEdit" asp-route-placeId="@warehouseFlower.PlaceId" asp-route-flowerId="@warehouseFlower.FlowerId" class="btn btn-primary">Редактировать</a>
                                    <a asp-action="FlowerDelete" asp-route-placeId="@warehouseFlower.PlaceId" asp-route-flowerId="@warehouseFlower.FlowerId" onclick="return confirm('Вы действительно хотите удалить цветы ?')" }) class="btn btn-danger">Удалить</a>
                                </span>
                            </div>
                        }
                    }
                    <div class="list-group-item">
                        <a asp-action="FlowerCreate" asp-route-id="@Model.Id" class="btn btn-secondary">Добавить</a>
                    </div>
                </div>
            </div>
            <div class="form-group">
                <a asp-action="Index" class="btn btn-danger">Назад</a>
                <input type="submit" value="Редактировать" class="btn btn-success" />
            </div>
        </form>

    </div>
</div>


